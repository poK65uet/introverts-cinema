import React, { useCallback, useEffect, useState } from 'react';
import { Helmet } from 'react-helmet-async';
import { useGetMessage } from 'queries/message';

export default function FilmManagementPage() {
  return (
    <div>
      <h2>Trang quản lý phim </h2>
    </div>
  );
}
